version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"

  app:
    build: .
    container_name: app
    ports:
      - "8085:8085"
    depends_on:
      - mongo
